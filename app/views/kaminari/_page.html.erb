<% if page.current? %>
  <span class="px-4 py-3 text-white bg-indigo-400 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "px-4 text-gray-800 font-light" %>
<% end %>
