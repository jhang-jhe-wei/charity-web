<%=
  image_url = "https://wells.tw/NTUST-Senior/docs/images/notify_tutorial.png"
  raw(
    Kamiflex.hash(self) do
      carousel do
        bubble size: :giga do
          body paddingAll: "0px" do
            image(
              image_url,
              size: :full,
              aspectRatio: "200:235",
              aspectMode: :fit,
              action: uri_action(image_url)
            )
          end
        end
      end
    end.to_json
  )
%>
